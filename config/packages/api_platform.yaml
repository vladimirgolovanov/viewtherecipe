api_platform:
    title: Hello API Platform
    version: 1.0.0
    defaults:
        stateless: true
        cache_headers:
            vary: ['Content-Type', 'Authorization', 'Origin']
    mcp:
        enabled: true
        format: jsonld
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
